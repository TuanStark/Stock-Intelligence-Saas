{
    "name": "@stock-intel/utils",
    "version": "0.1.0",
    "private": true,
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "build": "tsc",
        "type-check": "tsc --noEmit",
        "test": "jest",
        "clean": "rm -rf dist"
    },
    "devDependencies": {
        "typescript": "^5.7.3"
    }
}